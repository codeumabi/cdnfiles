// main.js — central loader for all scripts // This file loads scripts for the HOME PAGE (Brat Generator) (function () { // Base URL - this is the CDN link to your GitHub repository const base = "https://cdn.jsdelivr.net/gh/codeumabi/cdnfiles@main/scripts/"; // List of script files in CORRECT ORDER // IMPORTANT: Dependencies must load BEFORE files that use them const scripts = [ "font-loader.js", // 1. Load font loader first "presets.js", // 2. Load presets (color schemes, etc.) "utils.js", // 3. Load utility functions "state.js", // 4. Load StateManager (MUST be before app.js) "canvas.js", // 5. Load canvas rendering "export.js", // 6. Load export functions "gallery.js", // 7. Load gallery functions "app.js", // 8. Load main app (uses StateManager from state.js) "page.js" // 9. Load page-specific code last ]; // Loop through each script and add it to the page scripts.forEach(file => { const s = document.createElement("script"); s.src = base + file; // Creates full URL like: https://cdn.jsdelivr.net/gh/... s.async = false; // Load in order, NOT in parallel document.head.appendChild(s); }); })();
